# Copy to secrets.toml (same folder) for local run. Do not commit secrets.toml.
# Streamlit Cloud: paste under App settings → Secrets.

[supabase]
url = "https://YOUR_PROJECT.supabase.co"
key = "YOUR_ANON_PUBLIC_KEY"
